# This configuration file is user-editable and is mounted as a volume inside docker-compose.yaml, docker-compose-dev.yaml, and docker-compose-prod.yaml;
# in addition, it is merged with the JBrowse main configuration file (jbrowse.conf) through an include statement.
